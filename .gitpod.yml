# Uncomment this to use custom docker file => will take longer to open the workspace
# Also see: https://www.gitpod.io/docs/config-docker#custom-docker-image
# image:
#  file: .gitpod.Dockerfile 

# List the start up tasks. Learn more https://www.gitpod.io/docs/config-start-tasks/
tasks:
  # install python
  - before: pyenv install 3.10.4
  # install poetry and python dependencies; also see pyproject.toml
  - before: pip install poetry
    init: poetry install
    command: echo 'start script'

# List the ports to expose. Learn more https://www.gitpod.io/docs/config-ports/
#ports:
#  - port: 3000
#    onOpen: open-preview
